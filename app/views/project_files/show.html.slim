- content_for :title do
  = @title

- content_for :description do
  = @description

.status
  .lead-text
    = "Finished analyze of CSS."
    br
    = "You can check summary for CSS."
.contents.bottom
  .inner
    h2 Check result
    dl.clearfix
      dt.link
        = link_to "z-index", z_index_project_file_path(@project_file)
      dd Check z-index property which is used in the CSS.
    dl.clearfix
      dt.link
        = link_to "similarity", similarity_project_file_path(@project_file)
      dd Check similarity property which is used in the CSS.
    h2 Summary of this CSS
    dl.clearfix
      dt Selectors Num
      dd
        = @properties.count
        span selectors
